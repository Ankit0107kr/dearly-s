# Backend REST base (Backend runs on PORT=5001)
NEXT_PUBLIC_API_URL=http://localhost:5001/api/v1

# No Razorpay keys here, deliberately.
# The secret must never reach the browser, and the publishable key id is handed
# to the client by POST /payments/create. Keys live in Backend/.env only.
